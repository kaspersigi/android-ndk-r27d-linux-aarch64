diff --git a/logging.cpp b/logging.cpp
index b296d3b..7bc9e7e 100644
--- a/logging.cpp
+++ b/logging.cpp
@@ -61,6 +61,15 @@
 namespace android {
 namespace base {
 
+// __builtin_available(android ...) is a Clang extension.  The r27d Linux
+// AArch64 host build uses GCC, and on a non-Android host our liblog
+// compatibility layer always exposes the Android 30 entry points.
+#if defined(__clang__)
+#define LIBBASE_ANDROID_30_AVAILABLE() __builtin_available(android 30, *)
+#else
+#define LIBBASE_ANDROID_30_AVAILABLE() true
+#endif
+
 // BSD-based systems like Android/macOS have getprogname(). Others need us to provide one.
 #if !defined(__APPLE__) && !defined(__BIONIC__)
 static const char* getprogname() {
@@ -209,7 +218,7 @@ static std::recursive_mutex& TagLock() {
 static std::string* gDefaultTag;
 
 void SetDefaultTag(const std::string& tag) {
-  if (__builtin_available(android 30, *)) {
+  if (LIBBASE_ANDROID_30_AVAILABLE()) {
     __android_log_set_default_tag(tag.c_str());
   } else {
     std::lock_guard<std::recursive_mutex> lock(TagLock());
@@ -308,7 +317,7 @@ static void LogdLogChunk(LogId id, LogSeverity severity, const char* tag, const
   int32_t lg_id = LogIdTolog_id_t(id);
   int32_t priority = LogSeverityToPriority(severity);
 
-  if (__builtin_available(android 30, *)) {
+  if (LIBBASE_ANDROID_30_AVAILABLE()) {
     __android_log_message log_message = {sizeof(__android_log_message),     lg_id, priority, tag,
                                          static_cast<const char*>(nullptr), 0,     message};
     __android_log_logd_logger(&log_message);
@@ -390,7 +399,7 @@ LogFunction SetLogger(LogFunction&& logger) {
   LogFunction old_logger = std::move(Logger());
   Logger() = std::move(logger);
 
-  if (__builtin_available(android 30, *)) {
+  if (LIBBASE_ANDROID_30_AVAILABLE()) {
     __android_log_set_logger([](const struct __android_log_message* log_message) {
       auto log_id = log_id_tToLogId(log_message->buffer_id);
       auto severity = PriorityToLogSeverity(log_message->priority);
@@ -406,7 +415,7 @@ AbortFunction SetAborter(AbortFunction&& aborter) {
   AbortFunction old_aborter = std::move(Aborter());
   Aborter() = std::move(aborter);
 
-  if (__builtin_available(android 30, *)) {
+  if (LIBBASE_ANDROID_30_AVAILABLE()) {
     __android_log_set_aborter([](const char* abort_message) { Aborter()(abort_message); });
   }
   return old_aborter;
@@ -494,7 +503,7 @@ LogMessage::~LogMessage() {
 
   // Abort if necessary.
   if (data_->GetSeverity() == FATAL) {
-    if (__builtin_available(android 30, *)) {
+    if (LIBBASE_ANDROID_30_AVAILABLE()) {
       __android_log_call_aborter(msg.c_str());
     } else {
       Aborter()(msg.c_str());
@@ -509,7 +518,7 @@ std::ostream& LogMessage::stream() {
 void LogMessage::LogLine(const char* file, unsigned int line, LogSeverity severity, const char* tag,
                          const char* message) {
   int32_t priority = LogSeverityToPriority(severity);
-  if (__builtin_available(android 30, *)) {
+  if (LIBBASE_ANDROID_30_AVAILABLE()) {
     __android_log_message log_message = {
         sizeof(__android_log_message), LOG_ID_DEFAULT, priority, tag, file, line, message};
     __android_log_write_log_message(&log_message);
@@ -528,7 +537,7 @@ void LogMessage::LogLine(const char* file, unsigned int line, LogSeverity severi
 }
 
 LogSeverity GetMinimumLogSeverity() {
-  if (__builtin_available(android 30, *)) {
+  if (LIBBASE_ANDROID_30_AVAILABLE()) {
     return PriorityToLogSeverity(__android_log_get_minimum_priority());
   } else {
     return gMinimumLogSeverity;
@@ -539,7 +548,7 @@ bool ShouldLog(LogSeverity severity, const char* tag) {
   // Even though we're not using the R liblog functions in this function, if we're running on Q,
   // we need to fall back to using gMinimumLogSeverity, since __android_log_is_loggable() will not
   // take into consideration the value from SetMinimumLogSeverity().
-  if (__builtin_available(android 30, *)) {
+  if (LIBBASE_ANDROID_30_AVAILABLE()) {
     int32_t priority = LogSeverityToPriority(severity);
     return __android_log_is_loggable(priority, tag, ANDROID_LOG_INFO);
   } else {
@@ -548,7 +557,7 @@ bool ShouldLog(LogSeverity severity, const char* tag) {
 }
 
 LogSeverity SetMinimumLogSeverity(LogSeverity new_severity) {
-  if (__builtin_available(android 30, *)) {
+  if (LIBBASE_ANDROID_30_AVAILABLE()) {
     int32_t priority = LogSeverityToPriority(new_severity);
     return PriorityToLogSeverity(__android_log_set_minimum_priority(priority));
   } else {
